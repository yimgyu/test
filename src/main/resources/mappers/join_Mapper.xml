<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.mybatis_test3.Repository.User_join_Mapper">
    <insert id="insert_User" parameterType="com.example.mybatis_test3.Entity.User_info_Entity">
       insert into user_test2 (user_id, user_pw, user_name, user_name_uuid) values (#{user_id},#{user_pw},#{user_name},#{user_name_uuid});
    </insert>

    <select id="id_check" parameterType="String" resultType="java.lang.Integer">
        select COUNT(*) from user_test2 where user_id = #{user_id}
    </select>

    <select id="login" parameterType="com.example.mybatis_test3.Entity.User_info_Entity">
        select * from user_test2 where user_id=#{user_id} and user_pw=#{user_pw}
    </select>

    <select id="uuid_check" parameterType="String">
        select COUNT(*) from user_test2 where user_name_uuid = #{uuid}
    </select>
</mapper>